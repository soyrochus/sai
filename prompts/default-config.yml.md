See the file [../templates/default-config.yaml](../templates/default-config.yaml) for the standard
tools which are being created in the Global config file during application init with

```bash

> sai --init

```
